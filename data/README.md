# Data directory

This folder stores uploaded GIFs, processed conversions, and the JSON database used by the
FastAPI backend. The `.gitignore` file excludes runtime artefacts so that only this
explanatory file is tracked in version control.
